name = "fashion-store-api"
compatibility_date = "2024-01-01"
main = "src/index.js"

[vars]
ENVIRONMENT = "production"

# 邮件服务配置
EMAIL_SERVICE = "resend"
EMAIL_API_KEY = "re_bxmVM2pT_4pGb1LLK2HFvZN6QWuPi8hwq"
EMAIL_FROM = "no-reply@mail.saysno.com"  # 使用已验证的域名邮箱
ADMIN_EMAIL = "reluct007@gmail.com"  # 接收通知的管理员邮箱（可以是任何有效邮箱）

# 注意：
# 1. EMAIL_FROM 如果使用自定义邮箱（如 gmail.com），需要在 Resend 中验证域名
# 2. 测试时可以使用 onboarding@resend.dev（Resend 提供的测试邮箱）
# 3. 生产环境建议验证域名后使用自定义邮箱
# 详细配置说明请参考 EMAIL_CONFIG.md

# D1 数据库配置
[[d1_databases]]
binding = "DB"
database_name = "fashion-store-db"
database_id = "9f09384e-1afb-45ff-ac2a-11d34c11a986" # 需要在 Cloudflare Dashboard 中创建数据库后填入

# 注意：如果需要自定义路由，取消下面的注释并配置您的域名
# [[routes]]
# pattern = "api.yourdomain.com/*"
# zone_name = "yourdomain.com"

